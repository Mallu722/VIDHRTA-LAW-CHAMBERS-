# MongoDB Connection URI
# Replace this with your actual MongoDB connection string.
# (If using MongoDB Atlas, it will look like the one below)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/vidhrta?retryWrites=true&w=majority
